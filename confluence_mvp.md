Confluence - MVP Progress (publishable content)

Title: StudyBuddy — MVP Progress

Summary:
This page documents the Unit 12 MVP work and the use of AI tools and professional workflows during development.

MVP Artifacts:
- `app.py` — Minimal Flask app (UI + `/api/health`).
- `Dockerfile` and `requirements.txt` — Containerization for local/cloud builds.
- `.github/workflows/ci.yml` — CI workflow (syntax check + Docker build).

Public URLs (placeholders):
- Jira project (public): (paste your public Jira URL here)
- Confluence space (public): (paste this Confluence page URL here)
- Deployed app (Cloud Run): (paste Cloud Run URL here)

AI Tools Used:
- GitHub Copilot: assisted with code generation and Dockerfile suggestions.
- ChatGPT: drafted README content, commit message examples, and deployment notes.

Development Workflow Evidence:
- Feature branches were used for each new capability (example branch names listed in `commit_history_sample.md`).
- Pull requests were used to merge features into `main` with descriptive commit messages.

Next steps:
- Publish this content to your Confluence space and mark it public.
